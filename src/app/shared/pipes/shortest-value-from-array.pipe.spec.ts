import { ShortestValueFromArrayPipe } from './shortest-value-from-array.pipe';

describe('ShortestValueFromArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortestValueFromArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
