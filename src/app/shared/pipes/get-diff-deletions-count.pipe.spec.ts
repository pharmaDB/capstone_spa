import { GetDiffDeletionsCountPipe } from './get-diff-deletions-count.pipe';

describe('GetDiffDeletionsCountPipe', () => {
  it('create an instance', () => {
    const pipe = new GetDiffDeletionsCountPipe();
    expect(pipe).toBeTruthy();
  });
});
